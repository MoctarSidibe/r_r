# RabbitMQ Configuration for DGTT Auto-École
# Configuration principale pour la file de messages

# Network
listeners.tcp.default = 5672
management.tcp.port = 15672

# Default user and vhost
default_user = dgtt_user
default_pass = dgtt_password
default_vhost = dgtt_vhost

# Memory and disk limits
vm_memory_high_watermark.relative = 0.6
disk_free_limit.relative = 2.0

# Logging
log.console = true
log.console.level = info
log.file = false

# Management plugin
management.tcp.port = 15672
management.tcp.ip = 0.0.0.0

# Security
auth_mechanisms.1 = PLAIN
auth_mechanisms.2 = AMQPLAIN

# Clustering (for future scaling)
cluster_formation.peer_discovery_backend = classic_config

# Performance tuning
channel_max = 2047
frame_max = 131072
heartbeat = 60

# Queue and exchange settings
queue_master_locator = min-masters
default_user_tags.administrator = true

# Connection limits
connection_max = 1000
