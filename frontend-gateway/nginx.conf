# Micro Frontend Gateway Configuration
# Routes requests to appropriate micro frontends

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

upstream admin_frontend {
    server frontend-admin:80;
}

upstream candidate_frontend {
    server frontend-candidate:80;
}

upstream api_backend {
    server backend:80;
}

# Main server block
server {
    listen 80;
    server_name dgtt.local *.dgtt.local;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    
    # Admin micro frontend
    location /admin {
        proxy_pass http://admin_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Micro frontend specific headers
        proxy_set_header X-Micro-Frontend "admin";
        proxy_set_header X-User-Role "admin";
        
        # Timeout settings
        proxy_connect_timeout 10s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    # Candidate micro frontend
    location /candidate {
        proxy_pass http://candidate_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Micro frontend specific headers
        proxy_set_header X-Micro-Frontend "candidate";
        proxy_set_header X-User-Role "candidate";
        
        # Timeout settings
        proxy_connect_timeout 10s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    # API backend routing
    location /api {
        proxy_pass http://api_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # API specific headers
        proxy_set_header X-API-Version "v1";
        proxy_set_header X-Client-Type "web";
        
        # Timeout settings
        proxy_connect_timeout 10s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # Default route - serve a proper homepage
    location / {
        # Serve a professional homepage
        return 200 '<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DGTT Auto-École - Plateforme de Formation</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .container { 
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            padding: 60px 40px;
            text-align: center;
            max-width: 800px;
            width: 90%;
        }
        .logo { 
            font-size: 3rem;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }
        .subtitle {
            font-size: 1.2rem;
            color: #7f8c8d;
            margin-bottom: 40px;
            line-height: 1.6;
        }
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin: 40px 0;
        }
        .feature {
            padding: 30px 20px;
            border-radius: 15px;
            background: #f8f9fa;
            transition: transform 0.3s ease;
        }
        .feature:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }
        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
        }
        .feature h3 {
            color: #2c3e50;
            margin-bottom: 10px;
            font-size: 1.3rem;
        }
        .feature p {
            color: #7f8c8d;
            line-height: 1.5;
        }
        .btn-container {
            margin-top: 40px;
        }
        .btn { 
            display: inline-block;
            padding: 18px 35px;
            margin: 10px;
            background: linear-gradient(45deg, #3498db, #2980b9);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.3);
        }
        .btn:hover { 
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(52, 152, 219, 0.4);
        }
        .btn.admin {
            background: linear-gradient(45deg, #e74c3c, #c0392b);
            box-shadow: 0 5px 15px rgba(231, 76, 60, 0.3);
        }
        .btn.admin:hover {
            box-shadow: 0 8px 25px rgba(231, 76, 60, 0.4);
        }
        .status {
            margin-top: 30px;
            padding: 20px;
            background: #d4edda;
            border-radius: 10px;
            color: #155724;
            font-weight: 500;
        }
        .footer {
            margin-top: 40px;
            color: #7f8c8d;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">🚗 DGTT Auto-École</div>
        <div class="subtitle">
            Plateforme moderne de formation et gestion des permis de conduire
        </div>
        
        <div class="features">
            <div class="feature">
                <div class="feature-icon">👨‍💼</div>
                <h3>Espace Administrateur</h3>
                <p>Gérez les formations, suivez les progrès des candidats et administrez la plateforme</p>
            </div>
            <div class="feature">
                <div class="feature-icon">🎓</div>
                <h3>Espace Candidat</h3>
                <p>Accédez à vos cours, tests et suivez votre progression vers l'obtention du permis</p>
            </div>
            <div class="feature">
                <div class="feature-icon">📊</div>
                <h3>Suivi en Temps Réel</h3>
                <p>Statistiques détaillées et suivi des performances pour une formation optimale</p>
            </div>
        </div>
        
        <div class="btn-container">
            <a href="/admin" class="btn admin">🔐 Espace Administrateur</a>
            <a href="/candidate" class="btn">🎯 Espace Candidat</a>
        </div>
        
        <div class="status">
            ✅ Système opérationnel - Tous les services sont en ligne
        </div>
        
        <div class="footer">
            <p>DGTT Auto-École © 2025 - Plateforme de formation professionnelle</p>
            <p>Serveur: 168.119.123.247 | Version: 1.0.0</p>
        </div>
    </div>
</body>
</html>';
        add_header Content-Type text/html;
    }
    
    # Health check endpoint
    location /health {
        access_log off;
        return 200 '{"status": "healthy", "timestamp": "$time_iso8601", "service": "frontend-gateway"}';
        add_header Content-Type application/json;
    }
}

}
